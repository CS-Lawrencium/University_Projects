//课本p128：使用穷举法实现韩信点兵
//*虽然程序得到了正确的结果，但实际上纯属偶然，如果真正的解不在这个区间内，程序将一无所获

#include <stdio.h>
int main()
{
    int x;
    for (x = 1; x < 3000; x++)
    {
        if (x % 5 == 1 && x % 6 == 5 && x % 7 == 4 && x % 11 == 10)
        {
            printf("x = %d\n", x);
        }
    }
    return 0;
}